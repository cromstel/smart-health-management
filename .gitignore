# AI editor directories
.clinerules/
.kilocode/
.roo/
.windsurf/
.cursor/
.kiro/
.qoder/
.augment/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Packages & Dependencies
node_modules
dist
dist-ssr

# Environment files — NEVER commit real secrets (.env.local was previously tracked and has been removed from the index)
.env
.env.*
!.env.example
!server/.env.example
*.local
*.bak

# Build artifacts & generated output
dev-dist
playwright-report/
test-results/
*.tsbuildinfo
lint_output.txt
.vitest-full-output.txt
*.txt
coverage/

# Database backups (may contain PHI) — never commit
server/backups/
backups/

# TLS certificates and private keys — never commit
server/certs/

# Local-only review notes — never commit
thoughts/

# Internal documentation — never commit (protected from public exposure)
docs/

# AI operating memory — local-only agent tooling
ai/

# Local-only scripts (e.g., secret generator)
scripts/

# Demo accounts (may contain PHI) — never commit
demo-accounts/*
demo-accounts/Demo Accounts - SMART HEALTH MANAGEMENT.md

# Playwright E2E specs — local dev only, not required in production
e2e/

# Editor directories and files
.vscode/*
.opencode/
opencode/
.trae/
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only plans and notes
todo/*
todo-plan.md
plan.md
dependabot-alerts.md

# Local-only markdown documentation — never commit (except README and CHANGELOG files)
*.md
!README.md
!src/docs/README.md
!docs/CHANGELOG.md
!.github/**/*.md

# Source docs site
src/docs/TROUBLESHOOTING.md
