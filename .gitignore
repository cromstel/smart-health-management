.clinerules/*
.kilocode/*
.roo/*
.windsurf/*
.cursor/*
.kiro/*
.qoder/*
.augment/*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Packages & Dependencies
node_modules
dist
dist-ssr

# Environment files — NEVER commit real secrets (.env.local was previously tracked and has been removed from the index)
.env
.env.*
!.env.example
!server/.env.example
*.local

# Build artifacts & generated output
dev-dist
playwright-report/
test-results/
*.tsbuildinfo
lint_output.txt
.vitest-full-output.txt
*.txt
coverage/

# Database backups (may contain PHI) — never commit
server/backups/
backups/

# Local-only review notes — never commit
thoughts/

# Internal documentation — never commit (protected from public exposure)
docs/

# Demo accounts (may contain PHI) — never commit
demo-accounts/
demo-accounts/Demo Accounts - SMART HEALTH MANAGEMENT.md

# Playwright E2E specs — local dev only, not required in production
e2e/

# Editor directories and files
.vscode/*
.opencode/*
!.opencode/agent/
!.opencode/skills/
!.opencode/command/
.trae/
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw? 
